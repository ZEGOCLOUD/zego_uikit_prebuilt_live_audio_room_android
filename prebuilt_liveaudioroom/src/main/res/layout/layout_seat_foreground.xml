<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="74dp"
  android:layout_height="74dp">

  <ImageView
    android:id="@+id/seat_empty_place_holder"
    android:layout_width="54dp"
    android:layout_height="54dp"
    android:scaleType="center"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

  <TextView
    android:id="@+id/seat_name"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:paddingEnd="4dp"
    android:layout_marginTop="4dp"
    android:paddingStart="4dp"
    android:gravity="center"
    android:ellipsize="end"
    android:maxLines="1"
    android:textColor="@color/black"
    android:textSize="10sp"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/seat_empty_place_holder" />

  <ImageView
    android:id="@+id/seat_icon_mic"
    android:layout_width="54dp"
    android:layout_height="54dp"
    android:scaleType="center"
    android:src="@drawable/audioroom_icon_mic_off_for_seat"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent" />

  <ImageView
    android:id="@+id/seat_icon_host"
    android:layout_width="39dp"
    android:layout_height="12dp"
    android:layout_marginTop="44dp"
    android:scaleType="center"
    android:src="@drawable/audioroom_icon_user_signage"
    app:layout_constraintLeft_toLeftOf="@+id/seat_icon_mic"
    app:layout_constraintRight_toRightOf="@+id/seat_icon_mic"
    app:layout_constraintTop_toTopOf="@+id/seat_icon_mic" />

</androidx.constraintlayout.widget.ConstraintLayout>